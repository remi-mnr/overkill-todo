import { TodoStatusIconPipe } from './todo-status-icon.pipe';

describe('TodoStatusIconPipe', () => {
  it('create an instance', () => {
    const pipe = new TodoStatusIconPipe();
    expect(pipe).toBeTruthy();
  });
});
